numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
xgboost>=2.0
scipy>=1.10
fastapi>=0.110
uvicorn>=0.27
joblib>=1.3
pyarrow>=14.0
matplotlib>=3.7

# OPTIONAL: only needed to place REAL phone-call payment confirmations (Twilio).
# The demo runs fully offline in SIMULATED mode without this dependency.
twilio>=9.0
